These are just the last-used settings from the dialog — you don't need to edit
them here.

- `deck` — deck name shown in the dropdown, or `""` for the whole collection.
- `include_new` — also answer new/unseen cards.
- `include_learning` — also answer learning cards due later today.
- `limit` — stop after this many cards; `0` means no limit.
